[package]
name = "app"
version = "0.1.0"
edition = "2021"

[lib]
crate-type=["cdylib", "rlib"]

[dependencies]
log = "0.4.22"
simple_logger = "5.0.0"
winit-vulkan = { path = "../winit-vulkan" }


[features]
default = []
android = ["winit-vulkan/android"]